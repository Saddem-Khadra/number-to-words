version: "3.9"
services:
  number-to-words:
    container_name: number-to-words
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: >
      sh -c "uvicorn main:app --host=0.0.0.0 --port=8000"
